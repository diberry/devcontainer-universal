{
  "name": "regex_node",
  "version": "1.0.0",
  "description": "A TypeScript ESM project to search markdown files for Node.js version references using regex.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "keywords": ["regex", "nodejs", "typescript", "markdown"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0"
  }
}
